[pytest]
; Añade la carpeta backend/ (donde está este archivo) a sys.path, para que
; los tests puedan hacer "from app.main import app" sin importar desde qué
; carpeta se ejecute el comando "pytest".
pythonpath = .
testpaths = tests
